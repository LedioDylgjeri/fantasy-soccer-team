<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
  <h1>Your Players</h1>
    <% players.forEach(player => { %>
      <h3><%= player.name %></h3> 
      <h3><%= player.position %></h3> 
      <h3><%= player.number %></h3> 
      <h3><%= player.starting %></h3> 
    <% }) %>
</section>

<%- include('../partials/footer') %>