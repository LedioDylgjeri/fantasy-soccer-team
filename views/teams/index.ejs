<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
  <h1>Teams</h1>
    <% teams.forEach(team => { %>
      <h3><a href="/teams/<%= team._id %> "><%= team.name %></a> </h3> 
    <% }) %>
</section>

<%- include('../partials/footer') %>